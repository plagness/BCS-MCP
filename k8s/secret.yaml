apiVersion: v1
kind: Secret
metadata:
  name: bcs-secrets
  namespace: ns-bcs
type: Opaque
stringData:
  # Пароль для подключения к PostgreSQL
  BCS_DB_PASSWORD: bcs_secret
  # Токен обновления для MCP (опционально)
  BCS_REFRESH_TOKEN: ""
  # HTTP токен для MCP (опционально)
  MCP_HTTP_TOKEN: ""
